#!/bin/bash
echo "Welcome to GlobaleTraining.com"

a=10
b=3

sum=$(( a + b))
echo "Sum: ${sum}"

sub=$(( a - b ))
echo "Subtract: ${sub}"

multi=$(( a * b ))
echo "Multiply: ${multi}"

div=$(( a / b ))
echo "Division: ${div}"

mod=$(( a % b ))
echo "Modulus: ${mod}"